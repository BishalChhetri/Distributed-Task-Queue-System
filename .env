
DISPATCHER_HOST=0.0.0.0
DISPATCHER_PORT=5000

DB_PATH=dispatcher/queue_db.db

DEFAULT_MAX_ATTEMPTS=5
DEFAULT_LEASE_DURATION=100     # seconds (how long a worker can hold a task)

HEARTBEAT_MONITOR_INTERVAL=2    # seconds (how often to check for stale tasks and mark dead workers)
WORKER_DEATH_TIMEOUT=2        # seconds (worker marked dead after this many seconds without heartbeat)

WORKER_DISPATCHER_URL=http://localhost:5000
WORKER_ID=worker_auto              # Use 'worker_auto' for auto-generation

WORKER_POLL_INTERVAL=5           # seconds (how often to poll for tasks)
WORKER_HEARTBEAT_INTERVAL=1     # seconds (how often to send heartbeats)
 
USE_FORK_EXECUTION=False
CHECKPOINT_ENABLED=False
CHECKPOINT_INTERVAL=100000
CHECKPOINT_DIR=checkpoints

CACHE_DIR=cache

CACHE_TTL_SECONDS=3600            # 1 hour (cache expires after this)

CACHE_RETRY_INTERVAL=5         # seconds (how often to retry cached results)


LOG_LEVEL=INFO
ENABLE_DEBUG_LOGGING=False

DB_TIMEOUT=5                      # seconds

PRIMES_DEFAULT_METHOD=trial_division       # 'sieve' or 'trial_division'
PRIMES_MAX_LIMIT=100000000     # maximum limit for prime finding